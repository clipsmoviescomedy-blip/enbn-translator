# Dictionary database

en_bn_translator.db is a prebuilt SQLite file that Room copies in via
createFromAsset() on first launch. It is generated from a CSV word list.
The starter dictionary needs to be added here as a binary file separately.
